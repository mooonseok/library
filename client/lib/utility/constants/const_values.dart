import 'dart:math' as math;

abstract class ConstValues {
  const ConstValues();
  static const radius = math.pi / 180;
  static const double standardScreenWidth = 800;
  static const double standardScreenHeight = 1340;
}