import 'dart:math' as math;

abstract class ConstValues {
  const ConstValues();
  static const radius = math.pi / 180;
  static const double standardScreenWidth = 600;
  static const double standardScreenHeight = 800;
}
